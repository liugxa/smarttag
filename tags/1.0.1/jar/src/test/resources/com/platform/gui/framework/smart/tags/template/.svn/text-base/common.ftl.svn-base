<#assign sortBy="${context.parameters.sortBy!cells[0].name}">
<#assign sortOrder="${context.parameters.sortOrder!'asc'}">

<#if context.action?index_of('?') != -1>
	<#assign url="${context.action}&">
<#else>
	<#assign url="${context.action}?">
</#if>