<#include "common.ftl">
<form action="${getUrl(context)}" id="st_form_${context.name}" name="${context.name}" class="tableGridForm" method="post">
	<input type="hidden" name="st_name" value="${context.name}"/>
